# Plugins — DISABLED BY DEFAULT

Plugins are NOT loaded unless explicitly enabled by the user.

Rules:
- No access to Secure Core memory
- No access to keys
- Must be signed
- Must be sandboxed
- Disabled in all default builds
